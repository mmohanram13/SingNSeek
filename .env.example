# ============================================================================
# SingN'Seek - Environment Configuration
# ============================================================================
# This file contains environment-specific secrets and credentials
# Static configuration (dimensions, models, etc.) is in config.yaml
# Copy this file to .env and update with your actual credentials
# ============================================================================

# ============================================================================
# ELASTICSEARCH CONFIGURATION
# ============================================================================

# Connection Type: Choose 'local' or 'cloud'
ELASTICSEARCH_CONNECTION_TYPE=local

# --- LOCAL ELASTICSEARCH SETTINGS ---
# For local Docker setup (e.g., elastic/start-local)
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_SCHEME=http

# Elasticsearch API Key (used for both local and cloud)
# For local: Generate with: POST /_security/api_key {"name":"singnseek","expiration":"365d"}
# For cloud: Create in Kibana > Stack Management > API Keys
ELASTICSEARCH_API_KEY=your-api-key-here

# --- CLOUD ELASTICSEARCH SETTINGS ---
# For Elastic Cloud deployments (uncomment and use instead of local settings)
# ELASTICSEARCH_CLOUD_ID=your-cloud-id-here
# ELASTICSEARCH_API_KEY is used for authentication (same as above)

# ============================================================================
# GOOGLE VERTEX AI CONFIGURATION
# ============================================================================

# Vertex AI Project Settings
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_REGION=us-central1

# Path to your service account JSON key file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================

# Enable debug logging
# DEBUG=true
